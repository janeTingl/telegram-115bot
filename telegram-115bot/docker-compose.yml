version: '3.8'

services:
  telegram-115bot:
    build: .
    container_name: telegram-115bot
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    environment:
      - TZ=Asia/Shanghai